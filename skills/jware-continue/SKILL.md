---
name: jware-continue
description: "Deprecated — use /jware-auto instead. Redirects to /jware-auto 1 cycle."
---

# JWare Solutions — Continue (Deprecated)

This skill has been replaced by `/jware-auto`. Jane now runs as the top-level orchestration intelligence.

**If you are reading this, redirect the user:**

> "`/jware-continue` has been merged into `/jware-auto`. Use `/jware-auto 1 cycle` for a single cycle, or `/jware-auto` for continuous execution."

Do NOT execute any orchestration from this file. Invoke the `jware-auto` skill instead.
